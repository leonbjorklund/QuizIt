import { extendTheme } from '@chakra-ui/react';

const colors = {
  primary: '#0A192F',
  secondary: '#132F59',
  accent: '#147849',
  button: '#FAF089',
  selected: '#325386',
};

export const theme = extendTheme({ colors });
